# ==============================
# Schéma d'environnement pour hch-back
# Copiez ce fichier en .env et renseignez des valeurs réelles pour la production.
# Ce fichier DOIT être versionné. Le fichier .env NE DOIT PAS être versionné.
# ==============================

# Exécution
# Mettre sur 'production' sur le serveur
NODE_ENV=production
# Port HTTP exposé par NestJS dans le conteneur
PORT=3001

# CORS
# Liste d'origines autorisées (séparées par des virgules)
# Exemple : https://app.example.com,https://admin.example.com
CORS_ORIGIN=http://localhost:3000

# Base de données (PostgreSQL)
# Utilisée par Prisma et l'application à l'exécution
# En Docker, l'hôte est le nom du service `db`
DATABASE_URL=postgres://admin:admin@db:5432/hch
#Migration
ADMIN_DATABASE_URL=postgresql://postgres:UnTresGrosMotDePasseAdmin@db:5432/hch_test?schema=app
# Paramètres initiaux du service Postgres (utilisés par le conteneur db)
# À changer en production et à garder secrets.
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin
POSTGRES_DB=hch

# Authentification / Sécurité
# Secret aléatoire robuste pour la signature des JWT (min. 32 caractères recommandé)
# Générer avec : openssl rand -base64 48
SECRET_KEY=change-me-in-prod

# Super admin (création automatique au premier démarrage si aucun n'existe)
# IMPORTANT: Fournir au minimum SUPERADMIN_EMAIL et SUPERADMIN_PASSWORD en production.
# Si un SUPER_ADMIN existe déjà, ces variables sont ignorées.
SUPERADMIN_EMAIL=
SUPERADMIN_PASSWORD=
# Valeurs par défaut raisonnables si non définies
SUPERADMIN_FIRSTNAME=Admin
SUPERADMIN_LASTNAME=Super
SUPERADMIN_PHONE=0000000000
